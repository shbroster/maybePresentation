{
  "name": "maybe",
  "private": true,
  "scripts": {
    "build": "rm -rf docs && slidev build && mv dist docs",
    "dev": "slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^0.40.3",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*",
    "slidev-theme-dracula": "^0.1.0"
  },
  "devDependencies": {
    "playwright-chromium": "^1.31.2"
  }
}
